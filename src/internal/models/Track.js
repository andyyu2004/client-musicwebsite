class Track {
  constructor(title, album, artist, genre, encoding, trackid) {
    this.title = title;
    this.album = album;
    this.artist = artist;
    this.genre = genre;
    this.encoding = encoding;
    this.trackid = trackid;
  }
}